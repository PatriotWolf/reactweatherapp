/*
 * WeatherComponent Messages
 *
 * This contains all the text for the WeatherComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WeatherComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WeatherComponent component!',
  },
});
